## Summary

## Component(s) affected

- [ ] Backend (Spring Boot, root `src/`)
- [ ] Frontend (`frontend/`)
- [ ] Infra / Docker / CI

## Related issues

Closes #

## How was this tested?

- [ ] `./mvnw test` passes (backend changes)
- [ ] `npm run lint` passes (frontend changes — see known ESLint issue in README)
- [ ] `npm run build` passes (frontend changes)

## Security checklist (if touching security/, ratelimit/, or idempotency/)

- [ ] No secrets committed
- [ ] No new endpoint exposed without an authentication/authorization review
- [ ] New environment variable documented in README.md and .env.example, if added
